---

kanban-plugin: board

---

## todo

- [ ] Compare-Tabelle (`Compare.tsx`): Rating + Status in einer Zeile, klickbar mit Overlay
  - Scope: nur Vergleichstabelle (`frontend/src/pages/Compare.tsx`) — Dashboard/`ProductCard.tsx` unverändert; `ProductDialog.tsx` bleibt Voll-Edit (Aktionen-Zeile)
  - Zusammenfassen: Rating- und Status-Zeilen zu einer Zeile verschmelzen (Vorschlag Label „Bewertung & Status“); pro Produkt-Zelle Sterne (`StarRatingDisplay`, `parseRating`) + Status-Badge (`statusBadgeClass`/`translateStatus`) — Vorschlag: Sterne oben, Badge darunter
  - Klick (geklärt: ganze Zelle): ganze Zelle der zusammengefassten Zeile öffnet das Overlay (klickbar gedreht: `cursor-pointer`/Hover-Feedback + Tastaturzugang via Button-Semantik, Fokus-Ring)
  - Dialog (geklärt: neuer Mini-Dialog): neue Komponente (z.B. `frontend/src/components/RatingDialog.tsx`) auf shadcn-`Dialog`-Bausteinen aus `@/components/ui/dialog` (wie `ProductDialog`); enthält nur `StarRatingInput` (aus `StarRating.tsx`), Status-`Select` (`ITEM_STATUSES` aus `@/lib/types`, gleiches Pattern wie `ProductDialog` Zeilen 83–97) und Kommentar-`Textarea`
  - Speichern (geklärt: Button): lokaler Form-State im Dialog, Speichern-Button schreibt per einem `mutate` (Muster `handleSaveProduct` in `Compare.tsx`); Abbrechen verwirft; Rating-Format `'⭐'.repeat(n)` wie `ProductDialog` Zeile 47 übernehmen
  - Kommentar: Feld ist `item.notes` (gleiches Feld wie heutige „Erfahrungen“-Zeile) — „Erfahrungen“-Zeile aus der Tabelle entfernen, Kommentar ist nur im Modal sichtbar/ editierbar
  - Bestand heute (nicht neu erfinden): `StarRatingDisplay`/`StarRatingInput` (`StarRating.tsx`), `statusBadgeClass` (`lib/status-style.ts`), `translateStatus`/`ITEM_STATUSES` (`lib/types.ts`), Bearbeitungs-State-Muster `dialog {open, index}` in `Compare.tsx`
  - Edge Cases: leeres Rating (0 Sterne — `StarRatingInput` toggelt bereits auf 0), leere Notizen (Placeholder statt „Keine Notizen vorhanden“ in der Tabelle, da Zeile entfällt), schmale Zellen (`min-w-48`) auf Mobile, Overlay während SSE-Toast/Reload
  - Akzeptanz: eine Zeile statt Rating+Status+Erfahrungen; Klick auf Zelle öffnet Modal; Rating/Status/Kommentar speicherbar und in Tabelle sichtbar (Kommentar nirgends in Tabelle); Frontend-Build ok, `npm test` grün

## progress

## done

- [ ] MCP-Integration sichtbar machen (`AppHeader` + neue Seite `/mcp`): Button im Header, Status + Setup-Anleitung, Tests
  - Ziel (geklärt: neue Seite): Header-Button führt auf eigene Route `/mcp` (app-weit ohne `?journey=`-Param, wie `/login`); kein Abschnitt in `/einstellungen` (die ist journey-gebunden: `frontend/src/pages/Settings.tsx` liest/schreibt Journey-Config)
  - Button-Platzierung: `frontend/src/components/layout/AppHeader.tsx` (obere Auth-Zeile neben Sync-Status/ThemeToggle oder Nav-Zeile bei Logo/Switcher — bei Umsetzung entscheiden, Mobile-Wrapping beachten); nur sichtbare Navigation anfassen, `useLoggedIn`-Gate und Routen-Guard in `frontend/src/App.tsx` (`Routes`, `allowedLoggedOut`) entsprechend um `/mcp` erweitern und entscheiden ob ausgeloggt sichtbar
  - Inhalt (geklärt: read-only, keine editierbaren Felder): Verbindungsstatus (Backend erreichbar? Tool-Liste aus `tools/list`), kopierbares `mcpServers`-JSON für Muse (vgl. `README.md`-Snippet mit `command node src/mcp/server.js` + `MCP_BASE_URL`), Kurzanleitung (Server per `npm run mcp` starten, Backend muss laufen, Default Port 3000)
  - Bestand heute (nicht neu erfinden): MCP-Server `src/mcp/server.js` (stdio, Tools `journey.get`/`journey.add_item`/`journey.crawl_link`), kein MCP-UI im Frontend (nur Toast bei Fremdschreibzugriff via SSE, `frontend/src/lib/journey-data-context.tsx`), keine `/mcp`-Route in `frontend/src/App.tsx`
  - Zu klären bei Umsetzung: braucht die Statusanzeige einen neuen Backend-Endpunkt (z.B. `GET /api/mcp-status` mit Version + Tool-Liste) oder reicht clientseitiges `fetch` gegen bestehende Routen; ob der Button ein Icon (Font Awesome Solid) oder Text-Label bekommt
  - Tests (geklärt: kein Frontend-Harness): `test/mcp.test.js` erweitern (nur falls neuer Endpunkt dazukommt), Klickpfad-Checkliste in `docs/local/MANUAL_TESTS.md` (Button sichtbar, `/mcp` erreichbar, Snippet kopierbar, Statusanzeige korrekt bei laufendem/gestopptem Backend)
  - Edge Cases: Backend gestoppt (Status rot, Anleitung trotzdem lesbar), Supabase nicht konfiguriert / ausgeloggt (Sichtbarkeit wie restliche Nav), `frontend/dist` fehlt
  - Akzeptanz: Header-Button springt auf `/mcp`, Seite zeigt Status + kopierbares Setup-Snippet + Anleitung; `npm test` grün, Frontend-Build ok, `oxlint` ohne Errors
  - Erledigt 2026-09-15: neue read-only Seite `/mcp` (`frontend/src/pages/Mcp.tsx`, app-weit ohne `?journey=`-Param, in `frontend/src/App.tsx` als Route + in `allowedLoggedOut` wie `/login` — auch ausgeloggt lesbar); Header-Button in der oberen Auth-Zeile von `AppHeader.tsx` (immer sichtbar, Font-Awesome-Solid-Icon `faPlug` + Text-Label „MCP“, Aktiv-Zustand auf `/mcp`). Offene Punkte entschieden: neuer öffentlicher Backend-Endpunkt `GET /api/mcp-status` in `src/web/backend/server.js` (Name/Version/Protokoll + Tool-Liste spiegeln `src/mcp/server.js`, ohne `authGate` wie `/api/config` — der Browser kann kein stdio-`tools/list` sprechen, ein erfolgreicher Fetch heißt zugleich „Backend erreichbar“); Frontend liest ihn via neuem `fetchMcpStatus()` in `frontend/src/lib/api.ts`, bei gestopptem Backend bleibt die Seite lesbar und nur der Status ist rot. Tests: 2 neue Tests in `test/mcp.test.js` (Status-Shape + Abgleich der Tool-Namen mit live-`tools/list`); `npm test` 66/66 grün, Frontend-Build ok, `oxlint` 0 Errors (keine Warnungen in berührten Dateien). Manuelle Browser-Verifikation ausstehend, siehe `docs/local/MANUAL_TESTS.md`.
  - Erledigt 2026-09-15 (Erweiterungen per Workflow): Single Source of Truth — neues `src/mcp/tools.js` (SERVER_INFO/Protokoll/TOOL_DEFS inkl. `inputSchema`), von `src/mcp/server.js` und Backend requiriert; `GET /api/mcp-status` liefert zusätzlich `inputSchema` je Tool sowie `serverFile`/`port`/`baseUrl`. `/mcp`-Seite: fertig einfügbares Snippet ohne Platzhalter, Pflichtparameter je Tool, „Erneut prüfen“-Button, „Lesepfad prüfen“-Selbsttest, ehrliche Status-Copy (`npm run mcp` läuft separat), mobiles Icon-only-Label. Tests in `test/mcp.test.js` auf inputSchema/serverFile/baseUrl + inputSchema-Abgleich mit `tools/list` erweitert; selbst verifiziert: `npm test` 66/66 grün, Frontend-Build ok, `oxlint` 0 Errors (keine Warnungen in berührten Dateien). Eine fehlerhafte README-Änderung des Workflows (Port 1337) wurde revertiert. Manuelle Browser-Verifikation ausstehend, siehe `docs/local/MANUAL_TESTS.md` (20 Punkte).

- [ ] Buying-Journey-Skill (REST): neue Journey per API anlegen
  - Scope: nur Anlegen — diese Karte baut ausschließlich "neue Journey anlegen" (kein Item-/Log-/Feedback-CRUD, kein Chat-Loop; das spätere Skillset für App-Steuerung per Chat kommt als eigene Folgekarte)
  - Skill-Form: bestehenden Skill `.agents/skills/buying-journey-manager/SKILL.md` erweitern (REST-Modus dazu, kein neuer Skill daneben; Direkt-SQLite-Skripte `src/agent/scripts/add-item.js`/`add-log.js` bleiben unberührt)
  - Backend: neuer expliziter `POST /api/journeys`-Endpunkt in `src/web/backend/server.js` (Slug + optionale Felder Phase/Budget/Zieldatum/Notizen); kein Lazy-Create-Missbrauch über `GET /api/data` (das heute via `ensureJourney` in `src/db/store.js` stillschweigend anlegt)
  - Skill spricht REST statt Direkt-DB: Basis-URL/Port konfigurierbar (Default Port 3000, vgl. `.env` `PORT`/`DB_PATH`), Backend muss laufen; klare Fehlermeldung wenn Backend down (statt stillem SQLite-Schreiben)
  - Bestand heute (nicht neu erfinden): `GET /api/journeys` listet, `POST /api/data` speichert zu existierendem Slug, `GET /api/data?journey=X` legt via `ensureJourney` an; Slug-Normalisierung heute im Frontend (`frontend/src/lib/journey-id.ts`, aus CreateJourneyDialog wiederverwendet) — für Skill/Backend übernehmen
  - Zu klären bei Umsetzung: Request-/Response-Schema des POST (Slug-Pflicht, Duplikat → 409 vs. Idempotent-200), Validierung (leerer/ungültiger Slug)
  - Edge Cases: Slug existiert bereits, leerer/ungültiger Slug, Backend nicht laufend, `DB_PATH`-Variante
  - Akzeptanz: Skill-Befehl legt neue Journey (z.B. `smartphone`) per REST an, `GET /api/journeys` listet sie, kein Direkt-DB-Schreiben im neuen Pfad; `npm test` grün
  - Erledigt 2026-09-14: neuer `POST /api/journeys` in `src/web/backend/server.js` (Slug-Pflicht + optionale Strings Phase/Budget/TargetDate/GeneralNotes/SectionTitle/ListTitle, Slug-Normalisierung wie `frontend/src/lib/journey-id.ts`; `201 {success, slug}`, `409` bei Duplikat, `400` bei fehlendem/leerem/ungültigem Slug oder nicht-String-Optionalen) plus `store.createJourney()` in `src/db/store.js` (Defaults via `defaultJourneyRow`, Duplikat wirft `JOURNEY_EXISTS`); `GET /api/data`-Lazy-Create unangetastet. Offene Punkte der Karte entschieden: Duplikat → 409 (nicht idempotent-200), leere/ungültige Slugs → 400 ohne `bike`-Fallback. Skill `.agents/skills/buying-journey-manager/SKILL.md` um REST-Abschnitt „2b“ erweitert (curl-Beispiele mit konfigurierbarer Basis-URL, Default Port 3000, Backend-muss-laufen-Hinweis, Slug-/Antwort-Regeln). Tests: 4 neue Tests in `test/server.test.js` (explizites Anlegen + Feld-Roundtrip, 409-Duplikat inkl. normalisiertem Doppel, 400-Fälle, Normalisierung); `npm test` 32/32 grün; Skill-Pfad zusätzlich live verifiziert (201/409/400 + Listen-Eintrag). Kein UI-Touch, daher kein `MANUAL_TESTS.md`.

- [ ] Startseite `/`: aktive Buying-Journeys listen + Neuer-Button
  - Platzierung: `/` wird Übersicht — ohne `?journey=`-Param Liste zeigen, mit `?journey=<slug>` wie bisher das Detail-Dashboard (`frontend/src/App.tsx` `Routes`, Detail-Pfade `/vergleich`, `/eigenschaften`, `/feedback` bleiben param-gebunden)
  - Umfang: alle Journeys 1:1 aus `GET /api/journeys` (slug-sortiert, heute `src/db/store.js` `listJourneys`), kein Aktiv-Filter (Phase ist Freitext, Default `Planning`, kein Enum)
  - Karten minimal: pro Journey nur Name/Icon (`iconForJourney`) + Öffnen-Link (`/?journey=<slug>`); kein Backend-Umbau, keine N+1 Detail-Calls
  - Neuer-Button: bestehenden Erstellen-Dialog aus `JourneySwitcher.tsx` wiederverwenden (nur Kürzel, gleiche Slug-Normalisierung, Lazy-Create via Navigation + `ensureJourney`); dortigen veralteten Dateinamen-Hinweis (`.buying-journey.md`, SQLite speichert keine Files) mit korrigieren
  - Edge: Lade-/Fehlerverhalten wie im Switcher (Fallback lokale `recent_journeys`); Leerzustand de facto nie leer (Backend sichert mind. `bike`)
  - Akzeptanz: `/` ohne Param listet alle Slugs, Öffnen springt ins Dashboard, Neuer-Button legt Journey an und navigiert dorthin
  - Erledigt 2026-09-14: `/` ohne `?journey=` zeigt neue `JourneysOverview`-Seite (Name/Icon + Öffnen-Link je Slug, nur `GET /api/journeys`, kein Backend-Umbau), mit Param weiter Detail-Dashboard (`frontend/src/App.tsx`); Erstellen-Dialog als `CreateJourneyDialog` extrahiert und in Switcher + Übersicht wiederverwendet (gleiche Slug-Normalisierung via neuem `frontend/src/lib/journey-id.ts`, Lazy-Create via Navigation + `ensureJourney`), veralteter `.buying-journey.md`-Hinweis korrigiert. Tests: neuer Backend-Test „GET /api/journeys lists every journey slug-sorted“ in `test/server.test.js` (Startseiten-Vertrag), `npm test` 28/28 grün, Frontend-Build ok, `oxlint` 0 Errors; manuelle Browser-Verifikation ausstehend, siehe `docs/local/MANUAL_TESTS.md`.

- [ ] SilverBullet-Referenzen entfernen (SQLite/JSON1 ist Backend)
  - Scope: Full cleanup — tote Laufzeit-Referenzen UND One-shot-Migrations-Infra entfernen (Migration gilt als abgeschlossen, SQLite ist einziges Backend)
  - AppHeader: SilverBullet-Button ersatzlos streichen — `frontend/src/components/layout/AppHeader.tsx` (Import + Hook-Aufruf + Button-Block), Hook-Datei `frontend/src/lib/use-silverbullet-url.ts` komplett löschen (Backend `GET /api/config` in `src/web/backend/server.js` liefert nur `{storage:'sqlite'}`, Button war dadurch ohnehin tot)
  - Migrations-Infra löschen: `scripts/migrate-from-silverbullet.js`, `src/core/sb-client.js` (nur vom Migrations-Skript genutzt), `test/sb-client.test.js`, `package.json`-Einträge (`description`, `scripts.migrate`, `sb-client.test.js` im Test-Skript) bereinigen
  - Doku/Meta mitsäubern: `frontend/index.html` (Meta-Description), `README.md` (Migrations-Abschnitt + Env-Hinweis), `docs/vault/README.md` (Hinweis App-Daten in SilverBullet), historische Kommentare in `src/agent/scripts/utils.js`, `src/db/store.js`
  - Vorher prüfen: kein weiterer Laufzeit-Caller von `makeRequest`/`SB_API_BASE_URL`/`SB_AUTH_TOKEN` außer Migrations-Skript + Test; `.env`-Variablen danach entbehrlich
  - Akzeptanz: Suche nach `silverbullet`/`silverBulletUrl`/`SB_API`/`SB_AUTH`/`sb-client` liefert null Treffer im Code (ausgenommen diese Karte als Historie); Frontend baut, `npm test` grün (ohne `sb-client.test.js`)
  - Erledigt 2026-09-14: Migrations-Infra gelöscht (`scripts/migrate-from-silverbullet.js` + leeres `scripts/`-Verzeichnis, `src/core/sb-client.js`, `test/sb-client.test.js`), `package.json` bereinigt (Description, `migrate`-Skript, Test-Skript), Doku/Meta gesäubert (`frontend/index.html`, `README.md`, `docs/vault/README.md`, Kommentar-Köpfe in `src/agent/scripts/utils.js` + `src/db/store.js`); zusätzlich eine Residual-Zeile in `.agents/skills/buying-journey-manager/SKILL.md` ent-SB-t. Abweichung: AppHeader-Button + `use-silverbullet-url.ts` waren in der Arbeitskopie bereits entfernt (nichts mehr zu tun), `GET /api/config` lieferte schon `{storage:'sqlite'}`. Verifikation: `npm test` 27/27 grün, Frontend-Build ok, `oxlint` 0 Errors (7 Warnungen in unberührten Dateien). Suche ohne Treffer außer dieser Karte; `.env` (git-ignoriert, mit live-Token) enthält noch `SB_API_BASE_URL`/`SB_AUTH_TOKEN` — bitte selbst entfernen/rotieren. Keine neuen Tests (reine Löschung; `sb-client.test.js` mit Modul entfernt). Kein UI-Touch, daher kein `MANUAL_TESTS.md`.
- [ ] Light-Theme (`frontend/src/index.css`): Kontrastfehler nach WCAG AA überall fixen
  - Scope: nur Kontrast fixen — kein Redesign, keine neuen Farben/Layouts; ausschließlich `:root`-Tokens in `frontend/src/index.css` anpassen, `.dark`-Block unberührt lassen
  - Maßstab: WCAG AA — 4.5:1 für normalen Text, 3:1 für großen Text/UI-Komponenten/Fokusringe; Messung pro Vorder-/Hintergrund-Paar, nicht per Augenmaß
  - Geltungsbereich: überall — alle Pages (`Dashboard`, `Compare`, `Specs`, `Feedback`, `JourneysOverview`), Layout (`AppHeader`, `AppShell`, `NavTabs`, `JourneySwitcher`), Dialoge/Dropdowns/Toasts/Tooltips, Badges/Buttons/Inputs/Selects/Tables aus `components/ui`
  - Bestand heute: Light-Theme = `:root`-Variablen in `frontend/src/index.css` (Background `#F7F5EF`, Muted `#EDE8DC`, Muted-Foreground `#6E7686`, Primary `#2E8B76` auf `#FBFAF7`, Destructive `#B85C29`, Hiviz `#CFE829`, Steel `#6E7686`, Border/Input `#E1DACA`); Umschaltung via `ThemeToggle.tsx` (`next-themes`), Default `system` in `App.tsx`
  - Verdachtspaare zuerst prüfen: `muted-foreground` auf `muted`/`background`, `primary`/`celeste`/`rust` auf ihren Foregrounds, `steel` auf hellem Grund, `secondary-foreground`/`accent-foreground` auf `secondary`/`accent`, Placeholder-/Disabled-/Border-Zustände
  - Edge Cases: Light-Theme erzwungen testen (System-Dark darf Fehler nicht verdecken); Hover-/Focus-/Disabled-States und `ring` mitprüfen; Dark-Theme-Kontraste nicht verschlechtern
  - Akzeptanz: alle Text-/UI-Paare im Light-Theme erfüllen WCAG AA, Frontend-Build ok, `npm test` grün
  - Erledigt 2026-09-14: nur `:root`-Tokens abgedunkelt (gleicher Farbton, kein Redesign): `primary`/`celeste`/`ring` `#2E8B76`→`#287A66`, `muted-foreground`/`steel` `#6E7686`→`#5F6777`, `destructive`/`rust` `#B85C29`→`#A65324`; alle 14 gemessenen Text-/UI-Paare erfüllen jetzt WCAG AA (vorher 8 darunter, z.B. Muted-auf-Muted 3.74→4.65, Celeste-auf-Card 4.14→5.16, Weiß-auf-Primary 3.97→4.95). `.dark`-Block unberührt. Abweichung: `border`/`input` `#E1DACA` bewusst unverändert (dekorative Rahmen, kein Text; 3:1 dort wäre ein Redesign — der Fokus-`ring` als Zustandsanzeige liegt bei 4.74:1); Hover `primary/80` verbessert (2.95→3.44, transienter State). Tests: neuer `test/light-contrast.test.js` (5 Tests inkl. Dark-Unberührt-Wächter), Testskript in `package.json` erweitert; `npm test` 37/37 grün, Frontend-Build ok, `oxlint` 0 Errors (7 Warnungen in unberührten Dateien). Manuelle Browser-Verifikation ausstehend, siehe `docs/local/MANUAL_TESTS.md`.



- [ ] Backend/MCP: Infos zu bestimmter Buying Journey per REST+MCP lesbar machen
  - Scope: komplett — ein Aufruf liefert Status (Phase/Budget/Zieldatum), Items (Name/Preis/Specs/Rating/Status/Notizen/Link), Journey-Logs, Journey-Specs, GeneralNotes, Feedback zu genau einem Journey-Slug
  - Nur Lesen — kein Schreiben über diese Schnittstelle (kein POST/PUT/DELETE im MCP-Tool; existierende POST-Endpunkte bleiben unberührt)
  - Form: eigener MCP-Server (stdio) im Repo, der die REST-API aufruft (kein Direkt-DB-Zugriff aus dem MCP-Server, Backend muss laufen, Default-Port 3000)
  - Bestand heute (nicht neu erfinden): `src/web/backend/server.js` hat `GET /api/journeys`, `GET /api/data?journey=X` (liefert status, journey, items, specs, generalNotes, headers, sectionTitle, listTitle), `GET /api/feedback?journey=X`; Daten via `src/db/store.js` (`listJourneys`, `getJourneyData`, `getFeedback`); kein MCP-Code im Repo (Suche nach `mcp`/`MCP` ohne Treffer)
  - Zu klären bei Umsetzung: neuen kombinierten Lese-Endpunkt (z.B. Journey+Feedback in einem) oder MCP-Server aggregiert `GET /api/data` + `GET /api/feedback`; Umgang mit unbekanntem Slug (heute legt `getJourney()`/`ensureJourney()` stillschweigend mit Defaults an — für Read-Zugriff 404 statt Auto-Anlage prüfen); Slug-Sanitizing beibehalten
  - Edge Cases: unbekannter/leerer Journey-Slug, leere Journey (keine Items/Logs), Backend nicht laufend, `DB_PATH`-Variante, `frontend/dist` fehlt
  - Akzeptanz: MCP-Tool (z.B. `journey.get`) liefert für `bike` das komplette Dokument; unbekannter Slug gibt definierten Fehler (kein stilles Anlegen); `npm test` grün
  - Erledigt 2026-09-14: neuer MCP-Server `src/mcp/server.js` (stdio, JSON-RPC 2.0, ohne Dependencies, per `npm run mcp` startbar; Basis-URL via `MCP_BASE_URL` bzw. `PORT`, Default Port 3000); einziges Tool `journey.get` liefert `{slug, status, journey, items, specs, generalNotes, headers, sectionTitle, listTitle, feedback}`. Offene Punkte entschieden: kein neuer Backend-Endpunkt (MCP aggregiert `GET /api/journeys` + `GET /api/data` + `GET /api/feedback`, Backend unberührt); unbekannter Slug → definierter `isError`-Fehler, leerer Slug → `-32602`, Existenzprüfung via `/api/journeys` vor jedem `/api/data`-Call → kein stilles Anlegen (Frontend-Lazy-Create erhalten); Slug-Sanitizing wie Backend (unsafe Zeichen raus + lowercase, aber ohne `bike`-Fallback); Backend down → `isError` mit klarer Meldung. Tests: neuer `test/mcp.test.js` (5 Tests: tools/list, vollständiges bike-Dokument, unbekannter Slug ohne Anlage, leerer Slug, Backend down), Testskript in `package.json` erweitert; `npm test` 42/42 grün; zusätzlich live verifiziert (Doc-Keys, Fehlertext, Journeys-Liste unverändert). Kein UI-Touch, daher `MANUAL_TESTS.md` unverändert.
- [ ] Neue Karte via Muse Code erstellt (2026-09-14)
  - Erledigt 2026-09-14: Testkarte ohne Arbeitsauftrag — kein Spec, keine Code-Referenzen, daher nichts zu implementieren; kein Code geändert, keine neuen Tests nötig (kein Verhalten). `npm test` 42/42 grün (unverändert). Kein UI-Touch, daher `MANUAL_TESTS.md` unverändert.
- [ ] change icons to font awesome font
  - Erledigt 2026-09-14: alle 46 Lucide-Icons in 18 Dateien auf Font Awesome Free umgestellt (`@fortawesome/react-fontawesome` + `free-solid-svg-icons`, für Stern-Outline zusätzlich `free-regular-svg-icons`); `lucide-react` deinstalliert, keine Referenzen mehr im Code. Icon-Maps (`journey-category.ts`, `spec-icons.ts`, `Compare`-Attribute) liefern jetzt `IconDefinition` und rendern via `<FontAwesomeIcon>`, dynamische `<Icon/>`-Stellen entsprechend umgebaut; Spinner (`animate-spin`) laufen über den `spin`-Prop, Größen-/Farbklassen unverändert. Offene Punkte einseitig entschieden (keine Rückfragen erlaubt): Free statt Pro (keine Lizenz vorhanden), Solid-Stil, 1:1-Mapping (u.a. Bike→faBicycle, GitCompare→faCodeCompare, LayoutDashboard→faTableColumns, Disc→faLifeRing, ShieldQuestion→faShieldHalved, OctagonX→faCircleXmark, Home→faHouse, Scale→faScaleBalanced), SVG-Komponenten statt Webfont (Größen-API bleibt stabil). Verifikation: `npm test` 42/42 grün, Frontend-Build ok, `oxlint` 0 Errors (7 Warnungen in unberührten Dateien); keine neuen Unit-Tests (reiner Icon-Swap ohne Verhaltensänderung, kein Frontend-Test-Setup). Manuelle Browser-Verifikation ausstehend (Icon-Sichtbarkeit + Baseline-Ausrichtung), siehe `docs/local/MANUAL_TESTS.md`.

- [ ] Legacy-MD-Kern entfernen (`src/core/parser.js`/`serializer.js`): kein Sync/Connect mit externen MD-Files mehr
  - Scope: Full delete — toter MD-Datei-Pfad ersatzlos streichen (Migration auf SQLite gilt als abgeschlossen, SQLite ist einziges Backend); kein Ersatz, keine Migration, kein Datei-IO-Kappen-bei-gleichzeitigem-Behalten (vom User verworfen)
  - Löschen: `src/core/parser.js` (`parseMarkdown`, `getJourneyEmoji` — nur von Serializer + Index-Re-Export genutzt), `src/core/serializer.js` (`serializeToMarkdown`), `src/core/index.js` (reines Re-Export-Barrel ohne eigene Logik)
  - Behalten: `src/core/item-format.js` (`starsFromRating`, `specsToString` — live genutzt von `src/web/backend/server.js` `/api/import-link` und `src/agent/scripts/add-item.js`, getestet via `test/item-format.test.js`); `frontend/src/lib/markdown-lite.tsx` (reines Rendern auf der Feedback-Seite, kein Datei-IO); `src/agent/prompts/product_extraction_prompt.md` (Crawl-Extraktion, kein MD-Datei-Sync)
  - Umverdrahten: `require('../../core')` in `src/web/backend/server.js` (Zeile 6) und `src/agent/scripts/add-item.js` (Zeile 2) direkt auf `require('../../core/item-format.js')` zeigen lassen; `src/agent/scripts/utils.js` nur um die `parseMarkdown`/`serializeToMarkdown`-Importe + Re-Exporte bereinigen (`getData`/`saveData` SQLite bleiben)
  - Tests/Doku mitsäubern: `test/roundtrip.test.js` löschen + aus dem `test`-Skript in `package.json` streichen; `README.md`-Zeile „Kein externer Sync nötig“ umformulieren; `.agents/skills/buying-journey-manager/SKILL.md` (§B Schritt 4, §2b) nur dort anfassen, wo sie MD-Datei-Sync suggeriert (Prompt-Referenz selbst bleibt); MCP-Kommentar in `src/mcp/server.js` (Zeile 232) bleibt (Extraktions-Prompt, kein Sync)
  - Keine neuen Tests (reine Löschung): dauerhafte Absicherung sind die bestehenden `test/item-format.test.js` + `test/store.test.js` (SQLite-Backend); `npm test` muss grün bleiben
  - Edge: `ensureJourney`/Lazy-Create unangetastet; keine Änderung an DB-Schema, API oder UI
  - Akzeptanz: Suche nach `parseMarkdown`/`serializeToMarkdown`/`getJourneyEmoji`/`require('../../core')` liefert null Treffer im Code (ausgenommen diese Karte als Historie); `npm test` grün, Frontend-Build ok
  - Erledigt 2026-09-15: `src/core/parser.js`, `src/core/serializer.js` und `src/core/index.js` gelöscht (nur noch `src/core/item-format.js`); `src/web/backend/server.js` und `src/agent/scripts/add-item.js` requiren jetzt direkt `../../core/item-format.js`, `src/agent/scripts/utils.js` um Import + Re-Exporte bereinigt (`getData`/`saveData` unverändert); `test/roundtrip.test.js` gelöscht und aus dem `test`-Skript in `package.json` gestrichen; `README.md`-Sync-Zeile umformuliert („Alle Daten liegen lokal in der SQLite-Datei …“). Abweichung: `.agents/skills/buying-journey-manager/SKILL.md` unangetastet — keine MD-Datei-Sync-Formulierung gefunden (nur SQLite/REST/Direkt-DB); MCP-Kommentar wie vorgegeben belassen. Keine neuen Tests (reine Löschung); `npm test` 61/61 grün, Frontend-Build ok, Referenz-Suche ohne Treffer außer dieser Karte. Kein UI-Touch, daher kein `MANUAL_TESTS.md`.

- [ ] AppHeader: Auth-Bar über Nav-Bar + globaler Login/Logout-Switch
  - Layout (geklärt: zwei Zeilen): `AppHeader.tsx` bekommt obere Auth-Zeile (Anmelden/Abmelden + E-Mail, ThemeToggle, Sync-Status) und darunter die Nav-Zeile (Logo + JourneySwitcher + NavTabs); Mobile-Wrapping beachten
  - Ausgeloggt-Scope (geklärt: nur Übersicht + Login): ohne Login nur `JourneysOverview` (`/` ohne `?journey=`) und `/login`; Switcher, Detail-Dashboard (`/?journey=`) und Vergleich/Eigenschaften/Feedback/Einstellungen sind versteckt
  - Gate (geklärt: echter Route-Guard): `Routes` in `App.tsx` leitet Ausgeloggte von Detail-Routen auf die Übersicht um; Schalter ist der bestehende `useAuth()`-User als einzige Quelle — Komponenten mit 2 Zuständen (`NavTabs`, `JourneySwitcher`, `AppHeader`, `Routes`) lesen nur diesen
  - Bestand heute (gelesen, nicht neu erfinden): einzeiliger Header (`AppHeader.tsx`), `NavTabs.tsx` zeigt immer alle 5 Tabs, `auth-context.tsx` liefert `user/loading/configured`, `router.tsx` liest Journey aus `?journey=` (Default `bike`), `Login.tsx` behandelt `loading/not-configured/user` bereits
  - Offen bei Umsetzung: Redirect-Ziel (`/` vs. `/login`, Vorschlag: `/`), Verhalten während `loading` (Ladezustand statt Flackern) und bei `configured=false` (Vorschlag: wie eingeloggt behandeln); ob `NavTabs` ausgeloggt ganz versteckt wird (Vorschlag: ja, Login-Button steht in der Auth-Bar)
  - Edge Cases: Direkt-URLs/`?journey=`-Deep-Links ausgeloggt, Platzierung von ThemeToggle/Sync-Status in der Auth-Zeile, Mobile-Layout der zwei Zeilen
  - Akzeptanz: ausgeloggt nur Übersicht + Login sichtbar, Detail-URLs landen auf der Übersicht; eingeloggt volle Navigation; Frontend-Build ok, `npm test` grün
  - Erledigt 2026-09-15: Großteil (Route-Guard in `frontend/src/App.tsx` mit Redirect nach `/` + Ladezustand, `useLoggedIn()` in `frontend/src/lib/auth-context.tsx` als einziger Schalter inkl. `configured=false` → wie eingeloggt, Neuer-Button in `JourneysOverview.tsx` nur eingeloggt) war aus Vorarbeit bereits im Code und wurde verifiziert; Restlücke geschlossen: `AppHeader.tsx` umgebaut — obere Zeile ist jetzt reine Auth-Bar (Sync-Status, Reload, ThemeToggle, E-Mail/Anmelden/Abmelden, rechtsbündig), Logo steht in der unteren Nav-Zeile neben JourneySwitcher + NavTabs (ausgeloggt nur Logo, Switcher/Tabs versteckt). Keine neuen Unit-Tests (kein Frontend-Test-Setup; Backend unberührt); `npm test` 61/61 grün, Frontend-Build ok, `oxlint` 0 Errors (keine Warnungen in berührten Dateien). Manuelle Browser-Verifikation ausstehend, siehe `docs/local/MANUAL_TESTS.md`.

- [ ] Backend→Frontend Live-Updates (SSE): aktive Journey bei MCP-Schreibzugriff per Toast+Reload aktualisieren
  - Transport (geklärt: SSE-Push): Backend bekommt einen SSE-Endpunkt pro Journey (z.B. unter `/api/…`), Frontend subscribed per aktiver Journey (Einstiegspunkt `journey-data-context.tsx`); kein Polling-Intervall, kein manueller Reload als Dauerlösung
  - Scope (geklärt: aktive Journey): live geht nur das Dokument der gerade geöffneten Journey (`GET /api/data?journey=X`-Inhalt: Status/Logs/Items/Specs/Notizen/Titel); Feedback (`/api/feedback`), Configs (`/api/journey-config`) und die Journeys-Übersicht bleiben draußen (ggf. Folgekarte)
  - Konflikt (geklärt: Toast+Reload): bei eingehendem Event kein stilles Auto-Reload — Toast (z.B. „Neue Daten vom MCP-Server“) mit Reload-Button ruft das bestehende `load()`/`reload` auf; laufende Edits/Debounce-Saves (`mutate`/`mutateDebounced`, `AUTOSAVE_DELAY 600`) werden nie überschrieben, solange der Nutzer nicht bestätigt
  - Bestand heute (gelesen, nicht neu erfinden): `src/web/backend/server.js` ist reines Express-REST ohne SSE/WS; `src/mcp/server.js` (`journey.add_item`) macht Read-Modify-Write via `GET` + `POST /api/data` ohne jede Benachrichtigung; `frontend/src/lib/journey-data-context.tsx` lädt einmal pro Journey (`fetchJourneyData`) und persistiert nur bei eigenem `mutate`, `frontend/src/lib/api.ts` hat nur Fetch-Helper
  - Offen bei Umsetzung: Event-Name/Payload des SSE-Streams (z.B. Versions-Bump), welche Schreibpfade triggern (`POST /api/data` sicher; `/api/import-link`/`/api/feedback` bewusst draußen lassen oder mit aufnehmen), Auth am SSE-Stream (`authGate`/Bearer bei `AUTH_REQUIRED=true`), Reconnect/Backoff und Verhalten bei Journey-Wechsel sowie bei mehreren offenen Tabs
  - Edge Cases: Backend down/neustart (SSE-Reconnect), Event während In-Flight-Save oder offenem Dialog, MCP-Schreibzugriff auf andere als die geöffnete Journey (kein Toast), leere Journey, `DB_PATH`-Variante
  - Akzeptanz: MCP-`add_item` auf geöffneter Journey zeigt Toast, Reload übernimmt das neue Item ohne Seiten-Reload; ohne Bestätigung geht keine Editierung verloren; Schreibzugriff auf andere Journey stört nicht; `npm test` grün, Frontend-Build ok
  - Erledigt 2026-09-15: SSE-Push pro Journey — neuer `GET /api/data/events?journey=X` in `src/web/backend/server.js` (Event `journey-updated` mit `{slug, updatedAt}`, `retry: 5000`, 25s-Heartbeat, Cleanup bei `close`; mehrere Tabs je eigener Stream), `POST /api/data` broadcastet nach dem Speichern an die Clients derselben Journey; `POST /api/feedback`, `PUT /api/journey-config`, `POST /api/journeys` und `POST /api/import-link` bewusst draußen (Scope: nur `GET /api/data`-Inhalt). Auth: `extractBearerToken` in `src/web/backend/auth.js` akzeptiert für `EventSource` (keine Header möglich) `?token=`/`?access_token=` als Fallback, `authGate` sonst unverändert. Frontend: `frontend/src/lib/api.ts` mit `JOURNEY_UPDATED_EVENT` + `buildJourneyEventsUrl()` (Token via `getAccessToken`), `frontend/src/lib/journey-data-context.tsx` subscribed pro aktiver Journey und zeigt nur einen Toast („Neue Daten vom MCP-Server“, stabile Id, 30s) mit Reload-Button auf das bestehende `load()` — kein stilles Auto-Reload, Edits/Debounce-Saves gehen ohne Bestätigung nie verloren; Reconnect übernimmt der Browser, bei Journey-Wechsel wird geschlossen + Toast verworfen, Fremd-Journeys werden ignoriert. Offene Punkte entschieden: Event-Name `journey-updated`, nur `POST /api/data` triggert, Query-Token-Auth, Browser-Reconnect. Tests: neuer `test/sse.test.js` (ready + journey-updated bei POST, Stille bei fremder Journey, Stille bei Feedback-POST), Testskript in `package.json` erweitert; `npm test` 64/64 grün, Frontend-Build ok, `oxlint` 0 Errors (nur vorbestehende Warnungen). Manuelle Browser-Verifikation ausstehend, siehe `docs/local/MANUAL_TESTS.md`.

## waiting





%% kanban:settings
```
{"kanban-plugin":"board","list-collapse":[false,false,false,false]}
```
%%